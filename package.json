{
  "name": "govp-for-prestashop",
  "version": "0.1.0",
  "private": true,
  "license": "AFL-3.0",
  "scripts": {
    "lint": "find . -path ./vendor -prune -o -name '*.php' -print0 | xargs -0 -n1 php -l",
    "test:native": "bash tests/native-smoke.sh",
    "check": "npm run lint && npm run test:native",
    "package": "node scripts/package.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}

